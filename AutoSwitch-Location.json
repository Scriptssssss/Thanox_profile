[
  {
    "name": "AutoSwitchLocation",
    "description": "打开需要软件时开启定位，杀软件后关闭",
    "priority": 1,
    "condition": "if (frontPkgChanged == true && globalVarOf$location.contains(to)) {ui.showShortToast(\"EnableLocation......\" + (hw.enableLocation() ? \"Succeeded\" : \"Failed\"));} else if (pkgKilled == true && globalVarOf$location.contains(pkgName)) {ui.showShortToast(\"DisableLocation......\" + (hw.disableLocation() ? \"Succeeded\" : \"Failed\"));}",
    "actions": [
      ""
    ]
  }
]
